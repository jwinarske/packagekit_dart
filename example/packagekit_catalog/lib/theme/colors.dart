import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const seed = Color(0xFF1565C0);
  static const installed = Colors.green;
  static const available = Colors.blue;
  static const update = Colors.amber;
  static const security = Colors.red;
  static const removing = Colors.orange;
}
